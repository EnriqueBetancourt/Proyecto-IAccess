<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Información del visitante -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Imagen de visitante -->
        <ImageView
            android:id="@+id/imageIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_visitante"
            android:layout_marginEnd="8dp"
            android:contentDescription="Icono" />

        <!-- Nombre del visitante -->
        <TextView
            android:id="@+id/textNombre"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Nombre del visitante"
            android:textSize="16sp"
            android:ellipsize="end"
            android:singleLine="true" />

        <!-- CheckBox para seleccionar la visita -->
        <CheckBox
            android:id="@+id/checkBox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <!-- Detalle: Motivo de la visita -->
    <TextView
        android:id="@+id/textMotivo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Motivo de la visita"
        android:textSize="14sp"
        android:layout_marginTop="4dp"
        android:textColor="#808080" />

    <!-- Detalle: Hora de Entrada -->
    <TextView
        android:id="@+id/textEntrada"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Entrada: 12:00 PM"
        android:textSize="14sp"
        android:layout_marginTop="4dp"
        android:textColor="#808080" />
</LinearLayout>
